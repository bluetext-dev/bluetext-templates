apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - path: patches.yaml
  - target:
      kind: Ingress
      name: couchbase
    patch: |
      - op: replace
        path: /spec/rules/0/host
        value: couchbase.{{NAMESPACE}}.bluetext.localhost
      - op: add
        path: /spec/ingressClassName
        value: traefik
      - op: add
        path: /metadata/annotations
        value:
          traefik.ingress.kubernetes.io/router.entrypoints: web
