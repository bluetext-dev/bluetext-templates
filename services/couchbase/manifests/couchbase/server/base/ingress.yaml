apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: couchbase
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: couchbase.{{NAMESPACE}}.bluetext.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: couchbase
                port:
                  number: 80
