apiVersion: v1
kind: Service
metadata:
  name: curity--curity-admin
spec:
  type: ClusterIP
  selector:
    app: curity--curity
  ports:
    # Publish the admin interface port directly (no :80 indirection).
    - name: admin
      port: 6749
      targetPort: 6749
