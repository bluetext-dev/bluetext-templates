apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kong--kong
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: kong.$bt{{ deploy::namespace }}.bluetext.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kong--kong
                port:
                  number: 80
    - host: kong.$bt{{ deploy::namespace }}.bluetext.lvh.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kong--kong
                port:
                  number: 80
