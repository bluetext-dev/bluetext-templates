bucket_defaults:
  ram_quota_mb: 100
  num_replicas: 0
  flush_enabled: true

buckets:
  main:
    defaults:
      ram_quota_mb: 100
      bucket_type: couchbase
      flush_enabled: true
    scopes:
      _default:
        collections: {}
