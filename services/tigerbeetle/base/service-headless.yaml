apiVersion: v1
kind: Service
metadata:
  name: tigerbeetle-headless
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: tigerbeetle
  ports:
    - name: cluster
      port: 3000
      targetPort: 3000
